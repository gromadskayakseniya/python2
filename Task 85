n = int(input("Введите число: "))
a = list(map(int, input("Введите список: ").split()))

def increasing_after_removal(nums):
    def is_strictly_increasing(lst):
        for i in range(len(lst) - 1):
            if lst[i] >= lst[i + 1]:
                return False
        return True

    for i in range(n):
        if is_strictly_increasing(a[:i] + a[i+1:]):
            print("Да")
            return
    print("Нет")

increasing_after_removal(a)
